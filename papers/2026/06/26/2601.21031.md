# SIGMA-PPG: Statistical-prior Informed Generative Masking Architecture for PPG Foundation Model

**Arxiv:** 2601.21031 | **Date:** 2026-06-26 | **Area:** time-series

## What it does
SIGMA-PPG is a generative foundation model for PPG that replaces random masking in self-supervised pretraining with a learned masking policy. A reinforcement-learning teacher uses statistical priors of the signal (amplitude and skewness) to build a curriculum of hard masks in a teacher-student game, so the student cannot reconstruct trivially. The method reports better transfer to downstream PPG tasks than standard masked reconstruction.

## Direct parents (in Leo's library)
- [6INGKIJV] How Mask Matters / MAE Theory (zhang2023_mae) — analyzed why masked autoencoding works and how the mask ratio controls learning.
- [A5HBRQB9] Masked Autoencoders (MAE) — established high-ratio random masking for representation learning.

## Delta from parents
Where masked autoencoding picks masks at random and shows a high random ratio works for images, SIGMA-PPG argues random masking is the wrong choice for quasi-periodic signals, because the model fills a random gap by copying a neighboring beat and learns little. It therefore learns the mask as a policy, making the masking task as hard as the signal allows. This shifts the design knob from "how much to mask" to "where and how hard to mask", driven by signal statistics rather than a fixed ratio.

## Why Leo should care
This is bio-sensing pretraining. The imported method is masked autoencoding, and SIGMA-PPG pushes one step past it with a learned mask. Together with yesterday's HiMAE, masked reconstruction for PPG is now well covered, so the open direction is the masking policy itself, not the reconstruction objective.

## Verdict
Tier B TLDR
