# MemForest: An Efficient Agent Memory System with Hierarchical Temporal Indexing

**Arxiv:** 2605.23986 | **Date:** 2026-06-02 | **Area:** agent

## What it does
MemForest treats long-context agent memory as a write-efficient temporal data-management problem. It breaks the sequential update pipeline with parallel chunk extraction, and introduces MemTree, a time-ordered hierarchical index that replaces full-state rewrites with localized per-node updates along affected tree paths. On LongMemEval-S it reaches 79.8% pass@1, best among stateful baselines, with memory-construction throughput about 6 times higher than prior systems including EverMemOS. It is also evaluated on LoCoMo.

## Direct parents (in Leo's library)
- [BDY3HUCV] Memory Mechanisms Survey (Huang 2026) — cataloged memory mechanisms for foundation agents and the cost of state maintenance (graphify Community 0, gid huang2026_memory).

## Delta from parents
Where the survey catalogs memory designs and flags full-state rewrites as a scaling limit, this paper does the engineering answer: a temporal index with per-node updates so write cost scales with what changed rather than total memory size. The contribution is systems efficiency (throughput and latency) on top of the design space the survey described.

## Why Leo should care
The MemTree idea (a time-ordered hierarchical index with localized updates) maps onto long wearable signal histories, where new data arrives continuously and full recomputation is wasteful. It is an unported structure for biosignal foundation models that face the same continual-update setting.

## Verdict
Tier B TLDR.
